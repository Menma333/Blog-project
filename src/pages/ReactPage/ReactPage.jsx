import React from "react";
import CategoryPage from "../../components/CategoryPage/CategoryPage";

const ReactPage = ({ articles }) => {
  return <CategoryPage category="React" articles={articles} />;
};

export default ReactPage;
